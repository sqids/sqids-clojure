#!/usr/bin/env bash

set -euxfo pipefail

type="${1:-clj}"

case "$type" in
    clj)
        clj -M:dev:clj:nrepl
        ;;
    cljs)
        clj -M:dev:cljs:cljs/cli node-repl
        ;;
    *)
        : "Usage: bin/repl [clj|cljs]" >&2
        exit 1
        ;;
esac
